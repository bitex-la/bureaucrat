machine:
  pre:
    - sed -i 's/github/git-non-exist-hub/g' ~/.gitconfig # Fix cargo's github

dependencies:
  override:
    - curl -sf -L https://static.rust-lang.org/rustup.sh | sh /dev/stdin --channel=nightly --yes
test:
  override:
    - cargo test
