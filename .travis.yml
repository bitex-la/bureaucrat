language: rust
rust:
  - nightly
os:
  - linux
  - osx
addons:
  artifacts: true
  debug: true
  paths:
  - target/release/libbureaucrat*

script: cargo build --verbose && cargo test --verbose && cargo build --release --verbose && mv target/release/libbureaucrat.* target/ && rm -rf target/release && rm -rf target/debug/
